function positiveNumbers (ar) {
    const posArr = ar.filter(num => num > -1);
    let positiveNumbers = [-5, 10, -3, 12, -9, 5, 90, 0, 1];
    positiveNumbers.filter(posArr)
    return posArr
}

console.log()



/**let positiveNumbers = [-5, 10, -3, 12, -9, 5, 90, 0, 1];
getPositives(numArr);

// output: [10,12,5,90,0,1]

/**function getPositives(ar){
    const posArr = ar.filter(num => num > -1);
    
    return posArr;

console.log()*/